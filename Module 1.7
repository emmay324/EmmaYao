// Create a new Triangle object with a width of 4 and height of 5
    Triangle triangle = new Triangle(4, 5);
// Check that the area is calculated correctly (should be 10)
assert(triangle.getArea() == 10);

// Create a new Circle object with a radius of 3
        Circle circle = new Circle(3);
// Check that the area is calculated correctly (should be approximately 28.27)
        assert(Math.abs(circle.getArea() - 28.27) < 0.01);
